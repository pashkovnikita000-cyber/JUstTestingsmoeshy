---
phase: 01-bot-core-wallet-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - bot/__init__.py
  - bot/config.py
  - bot/database.py
  - bot/middleware.py
  - bot/handlers.py
  - bot/main.py
  - tests/__init__.py
autonomous: false
requirements: [AUTH-01, AUTH-02, DATA-01]
user_setup:
  - service: telegram
    why: "Бот-токен нужен, чтобы бот мог подключиться к Telegram API"
    env_vars:
      - name: BOT_TOKEN
        source: "@BotFather в Telegram -> /newbot -> токен"
      - name: ALLOWED_USER_IDS
        source: "Свой Telegram user id (узнать через @userinfobot), comma-separated"

must_haves:
  truths:
    - "Пользователь из whitelist отправляет /start и получает подтверждение работы бота"
    - "Пользователь НЕ из whitelist получает 'Access denied' без иных деталей"
    - "БД SQLite инициализируется при старте: существует таблица wallets с полем last_block"
    - "Бот запускается локально командой python -m bot.main без ImportError"
  artifacts:
    - path: "bot/config.py"
      provides: "Парсинг .env: BOT_TOKEN, ALLOWED_USER_IDS (set[int]), ETHERSCAN_API_KEY, DATABASE_PATH"
      contains: "ALLOWED_USER_IDS"
    - path: "bot/database.py"
      provides: "init_db() создаёт таблицу wallets; count_wallets(user_id)"
      contains: "CREATE TABLE IF NOT EXISTS wallets"
    - path: "bot/middleware.py"
      provides: "whitelist-проверка по user_id"
      contains: "Access denied"
    - path: "bot/main.py"
      provides: "Application setup + регистрация /start + init_db"
      contains: "run_polling"
  key_links:
    - from: "bot/handlers.py"
      to: "bot/middleware.py"
      via: "декоратор/проверка whitelist на /start"
      pattern: "whitelist|restricted"
    - from: "bot/handlers.py"
      to: "bot/database.py"
      via: "count_wallets в /start (real DB read)"
      pattern: "count_wallets"
    - from: "bot/main.py"
      to: "bot/database.py"
      via: "init_db при старте"
      pattern: "init_db"
---

<objective>
Walking Skeleton: тончайший сквозной срез, доказывающий работу всего стека.
Доверенный пользователь отправляет /start → whitelist middleware → чтение SQLite → ответ.

Purpose: Зафиксировать архитектуру (python-telegram-bot async + aiosqlite + .env whitelist), на которой строятся все последующие срезы. Реализует AUTH-01, AUTH-02, DATA-01.
Output: Запускаемый бот-пакет, схема БД (wallets + last_block per D-07), whitelist-доступ.
</objective>

<phase_goal>
**As a** доверенный пользователь, **I want to** запустить бота и получить ответ на /start, при этом чужие пользователи блокируются, **so that** я уверен, что бот работает и доступен только мне.

(Производная от ROADMAP Success Criteria 1-2; ROADMAP Goal-строка не в user-story формате — см. примечание планировщика.)
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bot-core-wallet-management/01-CONTEXT.md
@.planning/phases/01-bot-core-wallet-management/SKELETON.md
@CLAUDE.md

<library_docs>
Перед реализацией свериться через Context7:
- python-telegram-bot v21: Application.builder(), CommandHandler, ContextTypes.DEFAULT_TYPE, run_polling
- aiosqlite: connect, execute, commit, row factory
</library_docs>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Проверка легитимности pip-пакетов (security gate)</name>
  <what-built>RESEARCH.md с Package Legitimacy Audit отсутствует. Fallback-политика: все пакеты помечены [ASSUMED] и должны быть проверены человеком до установки (T-01-SC).</what-built>
  <action>НЕ устанавливать пакеты до подтверждения человеком. Это блокирующий security-гейт перед любым pip install. Дождаться "approved" от пользователя по списку пакетов ниже.</action>
  <how-to-verify>
    Открыть и подтвердить легитимность каждого пакета на PyPI:
    1. python-telegram-bot — https://pypi.org/project/python-telegram-bot/ (ожидается 21.x, поддержка активная)
    2. aiosqlite — https://pypi.org/project/aiosqlite/
    3. aiohttp — https://pypi.org/project/aiohttp/
    4. python-dotenv — https://pypi.org/project/python-dotenv/
    5. pytest, pytest-asyncio — https://pypi.org/project/pytest/ , https://pypi.org/project/pytest-asyncio/
    Проверить: имя точное (нет тайпсквоттинга), поддерживается, разумное число загрузок.
  </how-to-verify>
  <resume-signal>Напиши "approved" если все пакеты легитимны, либо укажи подозрительный</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold пакета + config + .env.example + requirements</name>
  <read_first>
    - CLAUDE.md (точная структура bot/ и список env vars)
    - .planning/phases/01-bot-core-wallet-management/SKELETON.md (архитектурные решения)
  </read_first>
  <files>requirements.txt, .env.example, bot/__init__.py, bot/config.py, tests/__init__.py</files>
  <action>
    Создать `requirements.txt` с закреплёнными версиями: `python-telegram-bot==21.*` (использовать конкретную последнюю 21.x), `aiosqlite`, `aiohttp`, `python-dotenv`, и dev: `pytest`, `pytest-asyncio`.
    Создать `.env.example` с ключами без значений: `BOT_TOKEN=`, `ETHERSCAN_API_KEY=`, `ALLOWED_USER_IDS=`, `DATABASE_PATH=./data/wallets.db`.
    Создать пустые `bot/__init__.py` и `tests/__init__.py`.
    Создать `bot/config.py`: загрузить `.env` через `dotenv.load_dotenv()`, экспортировать модульные константы `BOT_TOKEN: str`, `ETHERSCAN_API_KEY: str`, `DATABASE_PATH: str` (default `./data/wallets.db`), и `ALLOWED_USER_IDS: set[int]` — распарсить comma-separated строку в множество int (пустые значения отфильтровать). Реализует AUTH-01 (источник whitelist) и INFRA-02 (конфиг через .env).
  </action>
  <verify>
    <automated>python -c "import bot.config" 2>&1 | grep -v Error || echo OK; test -f requirements.txt && test -f .env.example && grep -q "ALLOWED_USER_IDS" bot/config.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` содержит строку с `python-telegram-bot==21` (префикс)
    - `.env.example` содержит `BOT_TOKEN=`, `ETHERSCAN_API_KEY=`, `ALLOWED_USER_IDS=`, `DATABASE_PATH=`
    - `bot/config.py` содержит `ALLOWED_USER_IDS` и парсит её в `set[int]`
    - `python -c "import bot.config"` не падает с ImportError (при наличии .env или дефолтов)
  </acceptance_criteria>
  <done>Пакет импортируется, конфиг читает .env, whitelist доступен как set[int]</done>
</task>

<task type="auto">
  <name>Task 3: SQLite schema + whitelist middleware + /start + main entry</name>
  <read_first>
    - bot/config.py (только что создан — ALLOWED_USER_IDS, DATABASE_PATH)
    - .planning/phases/01-bot-core-wallet-management/01-CONTEXT.md (D-07 last_block в схеме уже в Phase 1)
    - CLAUDE.md (структура и env)
  </read_first>
  <files>bot/database.py, bot/middleware.py, bot/handlers.py, bot/main.py</files>
  <action>
    `bot/database.py`: async `init_db()` — создаёт каталог из DATABASE_PATH при необходимости и выполняет `CREATE TABLE IF NOT EXISTS wallets (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER NOT NULL, address TEXT NOT NULL, name TEXT NOT NULL, last_block INTEGER DEFAULT 0, UNIQUE(user_id, address))`. Поле `last_block` добавляется уже сейчас per D-07 (Phase 2 читает без миграции). Реализует DATA-01. Также async `count_wallets(user_id: int) -> int` (real DB read для /start). Использовать aiosqlite, путь из config.DATABASE_PATH.
    `bot/middleware.py`: декоратор `restricted(handler)` — проверяет `update.effective_user.id in config.ALLOWED_USER_IDS`; если нет — отправляет ровно `Access denied` и возвращает управление (AUTH-02). Применяется ко всем командным хендлерам.
    `bot/handlers.py`: async `start(update, context)` под `@restricted` — вызывает `count_wallets(user.id)` и отвечает `✅ Bot is running. Tracking {n} wallets.` (real DB read замыкает стек).
    `bot/main.py`: async setup — `await init_db()`, `Application.builder().token(config.BOT_TOKEN).build()`, зарегистрировать `CommandHandler("start", start)`, `app.run_polling()`. Entry: `if __name__ == "__main__": main()` так, чтобы работало `python -m bot.main`.
  </action>
  <verify>
    <automated>python -c "import bot.database, bot.middleware, bot.handlers, bot.main"; grep -q "CREATE TABLE IF NOT EXISTS wallets" bot/database.py && grep -q "Access denied" bot/middleware.py && grep -q "run_polling" bot/main.py && grep -q "count_wallets" bot/handlers.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bot/database.py` содержит `async def init_db(` и `async def count_wallets(`
    - `grep "CREATE TABLE IF NOT EXISTS wallets" bot/database.py` находит строку с `last_block INTEGER DEFAULT 0`
    - `bot/middleware.py` содержит `Access denied` и проверку `ALLOWED_USER_IDS`
    - `bot/handlers.py`: `/start` вызывает `count_wallets` и отвечает строкой с `Bot is running`
    - `python -c "import bot.main"` импортируется без ошибок; `grep run_polling bot/main.py` находит вызов
    - Поведение: whitelisted user_id → ответ "✅ Bot is running..."; чужой → "Access denied"
  </acceptance_criteria>
  <done>Бот стартует через python -m bot.main, /start работает для whitelist, чужие заблокированы, БД создаётся</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user → bot | Любой может написать боту; user_id — единственный признак доверия |
| pip install → среда | Установка зависимостей из PyPI (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Любой Telegram-пользователь шлёт команды | mitigate | whitelist middleware `restricted` сверяет `effective_user.id` с ALLOWED_USER_IDS до выполнения любого хендлера |
| T-01-02 | Information Disclosure | Ответ чужому пользователю | mitigate | Ответ строго `Access denied` без деталей о боте/командах (AUTH-02) |
| T-01-03 | Information Disclosure | Утечка BOT_TOKEN/ключей | mitigate | Секреты только в `.env` (в `.gitignore`); в репо лишь `.env.example` без значений |
| T-01-SC | Tampering | pip installs (supply chain) | mitigate | RESEARCH.md audit отсутствует → fallback [ASSUMED]; блокирующий human-verify checkpoint (Task 1) до установки |
</threat_model>

<verification>
- `python -c "import bot.main"` — без ImportError
- `grep "CREATE TABLE IF NOT EXISTS wallets" bot/database.py` содержит `last_block`
- Ручная проверка: whitelisted /start → "✅ Bot is running...", не-whitelisted → "Access denied"
- `.env` присутствует в `.gitignore` (создать при необходимости)
</verification>

<success_criteria>
- Бот запускается локально (`python -m bot.main`)
- /start работает для пользователя из whitelist, блокируется для остальных
- SQLite инициализируется со схемой wallets (включая last_block)
- AUTH-01, AUTH-02, DATA-01 реализованы
</success_criteria>

<output>
Create `.planning/phases/01-bot-core-wallet-management/01-01-SUMMARY.md` when done
</output>
